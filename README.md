# Grok Chatbot (Streamlit)

A simple chatbot UI built with Streamlit and Grok (xAI) API.

## Features
- Chat-style UI
- Session-based memory
- Environment variable support

## Setup
```bash
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
